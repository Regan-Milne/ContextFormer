# Assumptions ledger

Every default we've inherited or introduced, tagged with where it came from,
what evidence exists, and the cheapest decisive test. Rule of engagement: spend
questioning budget strictly by test cost, cheapest first. A default is not a
conclusion; promotion requires an experiment that could have failed.

Status codes: **UNTESTED** / **WOBBLED** (evidence against, not decisive) /
**SUPPORTED** (evidence for, not decisive) / **FALSIFIED** / **CONFIRMED**
(decisive within current scope: one small model, short contexts).

| id | assumption | origin | status | evidence so far | cheapest decisive test | cost |
|----|-----------|--------|--------|-----------------|------------------------|------|
| A1 | KV should be compressed per layer | field default (cache memory layout promoted to method) | **FALSIFIED** | joint cross-layer PCA beats per-layer ~100x in KL at matched bytes (phase 1) | done | done |
| A2 | K and V deserve symmetric treatment | field default | **FALSIFIED** | K: low-rank but 2-bit catastrophic; V: near-full-rank but 4-bit nearly free (phase 1) | done | done |
| A3 | the KV image is the right object to store | field default (store what attention reads) | **WOBBLED** | trajectory-space (generator) compression wins at >=32x and survives int4 coeffs (phase 1) | sweep trajectory vs stack across models/docs | laptop-days |
| A4 | context conditioning enables compression (founding prior) | our hypothesis | **SUPPORTED (doc granularity)** | A5 test: per-doc basis beats foreign basis 24x in KL at matched bytes (0.011 vs 0.269 at 16x). Document-level context demonstrably buys compression. Token-level conditioning still untested (linear probe was weak) | causal predictive coder: predict token i's trajectory from *previous tokens' compressed states*, store only the residual; bits-of-residual vs unconditional coding at matched fidelity | 4090-days |
| A5 | per-document basis generalizes / is legitimate | our phase-1 shortcut | **RESOLVED: doc-specific** | foreign basis degrades KL 24x; corpus basis (5 diverse docs, held-out eval) recalls only 3/8 at 16x vs per-doc 8/8 at identical bytes, held-out KL 0.158 vs ~0.01. The enabling structure is constructed per document -- strongest evidence yet FOR A4 at doc granularity, and it fixes the accounting: 16x is marginal-only; net with 9.4MB fp16 basis = 6.4x @8k, 11.6x @32k, 14.6x @128k. Short contexts need a generated (learned) adaptation | done (analytic); learned encoder is the follow-on | done |
| A6 | every token deserves the same budget (uniform Z size) | our phase-1 simplification | **SUPPORTED (surprising)** | residual tail is THIN: median 0.092, p99 0.153, max 0.179 -- no expensive-token minority at the linear-codec level. Weak correlates (surprisal +0.26, log-freq -0.27). Adaptive two-tier LOST badly (KL 6.2 vs 0.53 uniform at matched bytes) -- confounded by A15 anomaly, but the thin tail independently says there's little to reallocate | retry adaptive with anomaly-free tiers after A15 is understood; re-test under learned codecs (tail may be codec-induced) | laptop-hours |
| A7 | mean KL captures the damage that matters | eval convention | **FALSIFIED** | at 16x, mean KL 0.033 "looks intact" but needle recall is 4/8; trajectory codecs recall 0/8 at KL ~0.6. Mean KL and exact recall dissociate completely. Every Pareto point now requires a recall column | done; recall harness now standing (a7_needle.py) | done |
| A8 | variance (PCA energy) is the right objective for subspace choice | tool default | **REFINED (C4)** | The catastrophic "variance metric" was specifically STANDARDIZED PCA (per-dim std division): std-384 recalls 1/8 (KL 0.47) where RAW PCA at 384 recalls 8/8 (KL 0.19) -- lower KL, worse recall (dissociation again). Raw magnitude-weighting ~= behavioral at 16-32x because magnitude correlates with query-probed K channels; behavioral weighting is the principled form and wins at stress (192: behavioral 2/8 > all others 0/8); non-diagonal wins the 32x typed battery (13/14 vs diag 11/14) but collapses at 64x (0/14). 64x is a hard wall for every linear codec AT 0.5B ONLY: at 4B behavioral 64x recalls 40/42 across three seeds at 8k and 14/14 at 16k, the wall does not transfer (2026-08-28/29). RESOLUTION REVISED at 32k (2026-08-30): the variance collapse is CONTEXT-LENGTH-dependent, not model-scale-dependent. At 4B/32k (fit-stride 2, same for both metrics) variance recalls 9/14 at 16x (NLL 0.319) and 5/14 at 32x (1.048) while behavioral holds 14/14 at both with sub-baseline NLL (0.011, 0.006). The 8k-16k "scale rescue" (variance 13-14/14 there) was a short-context rescue; stretch the evaluation length and the energy-ordered basis fails as at 0.5B. Note the discriminating variable is eval length itself: variance 16x@32k (9/14) matches its 16k full-fit sibling on fit samples (16000) and T/rank (3.5) yet fails where the 16k row passed. Behavioral remains never-worse in every row at every length | learned metric on 4090; understand nondiag's 64x collapse; 64x rows at 32k (in flight) | done (linear; 0.5B + 4B through 32k partial) |
| A9 | all layers matter equally (uniform rank across layers) | our phase-1 simplification | **UNTESTED** | layer-0 K is rank ~7; late-layer V ranks collapse (spectra) | leave-one-layer-out and per-layer rank allocation sweep | laptop-hours |
| A10 | recent tokens need full precision (exactness window) | field intuition | **UNTESTED** | none; phase 1 window tokens were uncompressed by construction (confound noted) | shrink/remove the exact window, measure | laptop-hours |
| A11 | compressed state must be expanded back to full KV before attention | our harness convention | **UNTESTED** | none; with a linear codec, attention can run *in latent space* (absorb basis into q/o projections, MLA-style), changing the compute Pareto entirely | derive absorbed form for the PCA codec, verify logits match expansion path | laptop-days |
| A12 | small-model findings transfer to larger models | necessity (hardware) | **SUPPORTED (tranche 1 complete)** | 4090 gate 2026-08-28/29, Qwen3-4B typed battery: 16k seed 11 behavioral 16x/32x/64x all 14/14 (gold-NLL 0.022/0.012/0.061 vs full 0.018); 8k three seeds behavioral 32x 42/42, 64x 40/42. Compressibility INCREASES with scale (the 0.5B 64x wall vanishes); the metric-separation finding transfers only as a fidelity margin (A8). SECOND FAMILY (Mistral-7B-Instruct-v0.2, 2026-08-29): transfer holds WITH a ratio-dependent tax -- 16x behaviorally free (13/14 = baseline, pre-registered), 32x-64x costs ~5/42 vs an imperfect 39/42 baseline; identifiers are the fragile type (sentencepiece many-piece spans); variance 64x 27/42 partially reproduces the small-model collapse, so the scale rescue is partly family-dependent. PREPRINT §8 second-family section has the tables | 32k; base-model Mistral variant to separate instruct effects | 4090-hours |
| A13 | fp16-GQA cache is the honest denominator for ratios | our accounting choice | adopted | GQA already bakes in 7x vs MHA; we quote on top of it | n/a (accounting policy, revisit if models change) | -- |
| A14 | pre-RoPE storage with rotation re-applied at read | our design choice | **SUPPORTED** | exact round-trip verified (phase 0); position free since tokens retained | none needed at this scope | done |
| A15 | joint-stack rank-192 anomaly is a bug/outlier interaction, not a real cliff | our guess | **RESOLVED** | the cliff was a variance-metric artifact: under the behavioral metric the sweep is strictly monotone (7.40 / 2.55 / 0.11 / 0.010 for ranks 96/192/384/768). Variance ordering half-reconstructs behaviorally-critical directions and misdirects attention | done | done |
| A16 | behavior degrades monotonically as rank drops (more components = better) | implicit in all rank sweeps | **FALSIFIED (variance ordering only)** | monotonicity restored under behavioral ordering; non-monotonicity was diagnostic of the wrong metric, not of the model | done | done |
| A18 | K-stack and V-stack can be compressed with separate bases (K deserves a private budget) | our A8 side-experiment | **FALSIFIED** | split codecs (K512/V256, K576/V192, behavioral metric, 16x) recall 0/8 with gold-NLL ~2.8 -- catastrophically worse than the joint codec at identical bytes. K and V of a token are strongly coupled; cutting the stack along the K/V seam is as destructive as cutting it along layers. The per-token whole-stack really is the natural object | understand the K-V coupling (shared components' K/V loadings) | laptop-hours |
| A17 | needle recall is depth-uniform | implicit | **RESOLVED (no systematic bias)** | the shallow-cluster pattern did not reproduce: B1 (24 randomized needles, 3 domains) recalls 24/24 at 16x; failures at 32x and at 7.5k context are scattered across depths. Original pattern was small-sample noise | done | done |
| A19 | the per-doc basis can be quantized (int8) to halve its storage | our accounting hope | **FALSIFIED** | int8 basis: 2/24 recall at 16x vs 24/24 with fp16 basis, identical coefficients. The basis is precision-critical like the keys it reconstructs. Net ratios must use fp16 basis (or a smarter basis-compression scheme than naive int8) | structured basis compression (low-rank of W, mixed precision by component) if net ratios at short contexts ever matter | laptop-hours |
| A20 | the basis works only for tokens it was fit on (deployment risk: decode-time tokens arrive after prefill fit) | our harness shape | **SUPPORTED w/ penalty (C1)** | basis fit on first 60% of doc, needles beyond the boundary: 7/8 recall (one miss = first needle past boundary), gold-NLL 0.229 vs 0.130. Forward generalization within a document works; decode-time encoding with a prefill basis is viable | quantify penalty vs distance past boundary at scale | 4090 |
| A21 | required rank grows linearly with context length | scaling fear | **WOBBLED toward sublinear (C2, 4090 tranche 1)** | 768 covers 1.9k (24/24); 1152 (+50%) recovers 7.5k (15/16) where 768 held 14/16; 1536 adds nothing -- the last miss is rank-INSENSITIVE (fails at 8x too, full KV passes). Rank demand grows much slower than T on this evidence; the residual failure mode is not capacity. 4B (2026-08-28/29): FIXED ranks 4608 and 2304 hold perfect recall at BOTH 8k and 16k (seed 11), doubling tokens-per-component with zero recall loss (NLL at 64x: 0.048 -> 0.061, budget felt but recall intact). 32k (fit-stride 2): rank 9216 14/14 (NLL 0.011) and rank 4608 14/14 (0.006, the project's best codec fidelity) -- rank 4608 now spans 8k/16k/32k perfectly, T/rank 1.7 -> 6.9. Strongly sublinear at 4B over a 4x context range (behavioral metric only; variance does NOT stretch, see A8) | rank 2304 at 32k, T/rank 13.9 (in flight); diagnose the rank-insensitive miss | 4090 |
| A22 | token-level causal context conditioning reduces per-token bits (founding hypothesis at token granularity) | our hypothesis | **NULL at linear capacity (C3)** | ridge prediction of Z_i from previous codes UNDERPERFORMS token identity alone (R^2 0.077 vs 0.177); at matched 384 B/tok, all conditional variants lose behaviorally to unconditional int4. Pattern with A5: context's benefit is document-GLOBAL (which subspace), not local-sequential (predict next code). Underpowered linear test -- not a verdict | nonlinear predictor + doc-summary-conditioned codec on 4090 | 4090 |
| A25 | useful low-dimensional KV geometry is fundamentally document-specific (A5b's verdict) | our A5b interpretation | **PRE-REGISTERED CHALLENGE (2026-08-29, before any sweep run)** | Hypothesis: A5b may have failed because the shared corpus basis was tested at excessive compression (16x), not because the geometry is document-specific. Design, registered in advance: shared PRE-FIT basis at 2x/4x/8x/16x on genuinely held-out heterogeneous documents (prose, technical text, dialogue, code/identifier-heavy, structured text as harness permits), each compared against a per-document basis at MATCHED rank; report recall, gold-NLL, typed failure profile, and the shared-vs-per-doc gap at every rank. Scientific question: is the subspace document-specific, family-specific, or substantially reusable across contexts? OUTCOME (2026-08-30, 0.5B primary, 5 held-out docs incl. one class with NO training representative, 70 needles/condition): CHALLENGE VINDICATED. Shared corpus basis: 68/70 at 2x AND 4x (baseline 69/70), 65/70 at 8x, 56/70 at 16x -- vs A5b's 3/8 at 16x. The old failure was substantially a configuration artifact (variance-flavored scaling + starved training pool), not fundamental doc-specificity. Decomposition: the useful geometry is SUBSTANTIALLY REUSABLE (shared basis nearly free through 8x, including on the never-seen structured class through 8x) PLUS a doc-specific increment that matters only under stress (per-doc 70/70 at 16x vs shared 56/70). Consequences: zero-per-session-baggage shared basis is viable at generous rank (liberal-rank direction validated at 0.5B); the stable basis DR progressive-bands needs exists; per-context adaptation is the price of aggressive ratios only. 4B confirmation within reachable ranks (8x/16x) pending | 4B confirmation; feed the corpus basis into DR experiments | done (0.5B primary) + 4B |
| A27 | token count is the right independent variable for required rank | implicit in every rank-vs-length sweep | **PRE-REGISTERED EXPERIMENT (2026-08-30, before any run)** | Question: does minimum behavior-preserving rank depend more on token count, information complexity, content type, or a measurable spectral/behavioral signal? Existing hints AGAINST raw T: rank 4608 held 14/14 across 8k/16k/32k on one distribution (near-flat in T within-content); Mistral identifier fragility (content modulates failure at fixed rank). Design: five filler classes at MATCHED token count (repetitive prose, ordinary prose, dense technical, code, identifier-heavy/structured), identical typed needles and placements, behavioral metric; one SVD per condition, nested-rank truncations via keep_ranks; find min rank preserving recall per condition; record each condition's behavioral-weighted spectrum decay + gzip/entropy as candidate predictors. Decision rule: if min-rank varies substantially (>2x) at fixed T across conditions, T is the wrong independent variable. R&D (dynamic rank controller) explicitly out of scope until this map exists. PILOT (pre-registered 2026-08-30, before running): two conditions at 8k, identical needles/placements/seed -- ordinary prose (mobydick, skip 30k) vs repetitive prose (one paragraph tiled, data/repetitive.txt), behavioral metric, ratios 128x/256x (ranks 1152/576, below the known-good 2304). Prediction if complexity drives rank: repetitive holds recall where prose degrades; if T drives it: both degrade together. A null (both hold at 256x) sends the sweep deeper before concluding. OUTCOME (same day): complexity-dependent WITH A FLOOR. Rank 1152: repetitive 14/14 (NLL 0.020) vs prose 12/14 (0.215), a 10x fidelity gap at matched everything; rank 576: BOTH fail (8/14 vs 6/14) despite the repetitive filler containing near-zero novel content. Min-intact-rank prose ~2304 vs repetitive ~1152: a 2x shift at fixed T, at the decision-rule threshold. Refined model: required rank ~ content-independent floor (needles are the only novel content in the repetitive condition; plus model-intrinsic per-token state) + content-driven term. Sweep design updated: isolate the floor (vary needle count / needle-free NLL variant) alongside the five content classes | five-class sweep + floor isolation (tonight, after the 32k variance-64x chunk) | 4090-hours |
| A26 | the codec must be fit on every context position (fit_len = T) | harness default | **PRE-REGISTERED DEVIATION (2026-08-29, forced)** | At 32k the fit matrix (32000 x 73728 fp32 = 2.36e9 elements) exceeds LP64 LAPACK's int32 addressing; CPU gesdd crashes with an illegal-argument assert (recorded verbatim in the chunk-1 log), GPU SVD needs ~48 GB VRAM, and the Gram-side eigh alternative squares the condition number in fp32 (rejected on A19 precision-criticality). Deviation: fit on every 2nd position (--fit-stride 2, identical gesdd code path, legal size), evaluate on ALL positions; disclosed in every affected row name. Prediction, registered before the validation run: at 16k, where both paths are runnable, stride-2 fit matches the published full-fit row within noise (+/-1 needle, ~0.01 NLL). Note the deviation is conservative for A21: a rank-R basis fit on 16k positions serving 32k positions bounds rank demand by the 16k requirement OUTCOME: recall matched exactly (14/14, identical type profile) but gold-NLL was 0.039 vs full-fit 0.012, +0.027, OUTSIDE the registered ~0.01 band -- half the fit samples measurably costs basis fidelity while leaving recall intact at this stress. Consequence for 32k reporting: recall rows are comparable across lengths; NLL rows at 32k carry a fit-stride confound vs the full-fit 8k/16k rows and are flagged as such. (The 32k fits use 16000 samples, 2x this validation's 8000, so the penalty there is bounded above by +0.027) | done (16k validation); the 32k battery | done + 32k |
| A24 | one preservation metric is optimal across content types and families | implicit until Mistral s13 | **PRE-REGISTERED HYPOTHESIS: falsified in favor of content-dependence** | Mistral 8k seed 13, 32x: behavioral 11/14 (identifiers 0/3, confusables 2/2) vs variance 12/14 (identifiers 3/3, confusables 0/2) -- complementary failure profiles, first recall row where variance beats behavioral. Registered BEFORE the s13 variance-64x row landed: if the 64x pair shows the same split (behavioral protects confusables, variance protects identifiers), this is a structural tradeoff, not noise. OUTCOME (s13 64x): prediction FAILED -- variance 7/14 with identifiers 0/3 (its 32x identifier advantage vanished; behavioral 12/14, identifiers 2/3). The complementary profile does not survive stress even within-seed; retained as an anomaly, and the metric-complementarity reading is DEMOTED. What survives: identifiers are the fragile TYPE for both metrics on Mistral (tokenization-multiplicative mechanism unrefuted), and behavioral's aggregate dominance under stress extends to the second family (8k 3-seed: behavioral 64x 35/42 vs variance 27/42). SECOND PRE-REGISTRATION (before the Mistral 16k rows landed): if behavioral 16x at 16k comes back near the 13/14 baseline, the Mistral tax is an operating-point problem (a more generous ratio suffices, supporting the liberal-rank + exactness-residual architecture); if identifiers still fail at 16x (T/rank ~2, mild stress), span exactness-fragility is the stronger explanation and the protected channel becomes load-bearing rather than optional. OUTCOME: operating-point verdict -- behavioral 16x = 13/14, NLL 0.329, per-type profile identical to baseline including the identifier miss. Compression is free at 16x on Mistral; the tax is ratio-dependent, not absolute. ALSO: the 32x complementary profile RECURRED at 16k (behavioral 2/2 confusable + 1/3 identifier vs variance 0/2 + 3/3, second independent occurrence, both at 32x) and the pre-registered 64x follow-up killed it AGAIN (behavioral 64x 13/14 with identifiers 3/3; variance 64x 8/14 broad losses). Adjacent-ratio non-monotonicity observed twice. Standing read: per-row metric profiles are noisy on this family; aggregates and NLL trends are the quotable objects (Mistral totals: behavioral 106/126 vs variance 91/126) Candidate mechanism: exact symbolic spans (snake_case identifiers) tokenize into many pieces under sentencepiece, recall requires every piece, so per-token fidelity compounds multiplicatively; prose-like needles are few-token + semantically redundant. Replacement claim: behavior-weighted compression improves average fidelity under stress, but the optimal metric is family- and content-dependent | per-type recall aggregated across seeds AND families; recall vs gold-token-count curve; type-aware allocation (or mixed-metric / protected residual channel for symbolic spans) vs single-metric at matched bytes | 4090-hours |
| A23 | a compression ratio quoted from a battery row is informative | implicit in all rank sweeps | **SCOPED (accounting rule)** | PCA rank <= T (sample count). Any row whose byte budget implies rank >= T is vacuous: the codec is near-exact by construction. Bit us live: the 4B 2k battery (rank capped at 1985 = T-8) returned every row identical to full KV; retained in reports/ as the cautionary example. Rule: informative rows need T comfortably above rank = stack_fp16_bytes/ratio (at 4B c8: T >> 9216 for 16x, 4608 for 32x, 2304 for 64x); quote T/rank alongside any cross-scale comparison, since matched RATIO does not match per-sample stress across stack sizes | none needed; standing accounting rule | done |

## Priority queue (updated 2026-08-23 after A5/A6/A7 results)

1. **A15/A16 anomaly** -- promoted to top: non-monotone rank-vs-behavior has now
   poisoned two experiments and blocks trustworthy allocation work. Understanding
   it likely resolves A8 too.
2. **A8 behavior-weighted subspaces** -- A16's falsification says energy ordering
   is actively wrong, not just suboptimal; recall (A7) may be recoverable at 16x+
   by selecting components for addressability instead of variance.
3. **A17 depth-dependent recall** -- cheap, and shapes how recall scores are read.
4. **A9 per-layer allocation** -- still likely free gains, now with recall column.
5. **A5 follow-up** -- corpus-fit global basis to separate domain from document.
6. **A11 latent-space attention** -- unchanged.
7. **A4 predictive coder** (4090) -- now with positive doc-level evidence; also the
   escape from the basis-storage amortization problem (generate the adaptation
   instead of storing it).
8. **A12 scale transfer** (4090).

## Dynamic Rank track (defined 2026-08-30)

Dynamic Rank studies how the minimum representational rank required to
preserve behavior changes over the lifetime of a context, and whether that
rank can be adjusted online as representational demand changes.

- **DR1: does rank demand vary by context composition at fixed T?**
  SUPPORTED (A27 pilot): intact-rank threshold shifts 2x (prose ~2304 vs
  repetitive ~1152), and the 128x row is a large effect (12/14 @ NLL
  0.215 vs 14/14 @ 0.020). Narrow claim earned: raw token count alone is
  not sufficient to predict required behavioral rank.
- **DR2: what variables determine rank demand?** Working capacity model to
  fit: r_min ~ r0 + alpha*N_needles + beta*C_context, where r0 =
  intrinsic/task/model floor, N = explicitly retrievable novel facts, C =
  complexity/novelty of surrounding context. The pilot floor is
  PROVISIONAL, not established as intrinsic: the repetitive condition
  still carries needles, position/ordering structure, query-retrieval
  relations, same-token-different-location state, and possibly
  irreducible model geometry. Instrument: five-class sweep x needle-count
  factorial.
- **DR3: does rank demand change locally over one session's lifetime?**
  Session-shaped document (prose -> technical -> code -> prose), fixed
  rank, per-region recall.
- **DR4: can that change be predicted cheaply enough to adjust rank
  online?** Candidate signals: per-token reconstruction residual in the
  retained band, behavioral-weighted mass outside the subspace, novelty
  of incoming states. Correlational validity first.
- The controller itself remains R&D, beyond DR4.

Reframing (2026-08-30): the track's question is no longer "how low can
rank go?" but "what determines the amount of representational capacity a
context actually deserves?"

Structural note: for a FIXED basis, nested PCA truncation is progressive
encoding (rank-r codes are prefixes of rank-r' codes; apply_joint
keep_ranks). "Increase rank" = retain another coefficient band; demoted
bands can be EVICTED to host RAM rather than discarded (composition with
write-time tiering), making rank a reversible residency decision. This
depends on basis stability: DR option-3 (progressive bands) requires the
shared pre-fit basis (A25) to hold; per-document bases would force
re-encoding on expansion instead.

## Standing rule (from A7's falsification)

No compression result is quotable from mean KL alone. Every Pareto point
carries: mean KL, top-1 agreement, **needle recall**, and gold-digit NLL.

**Honest frontier as of 2026-08-23 (behavioral metric, laptop gate passed):**
- 16x: behavioral joint-768 c8 -- recall 8/8, gold-NLL 0.130 (full KV: 0.123),
  KL 0.010, top-1 0.938. Effectively intact.
- 32x: behavioral joint-768 c4 or joint-384 c8 -- recall 7/8 (deepest needle
  lost), gold-NLL ~0.27.
- Superseded: variance-metric points (16x held only 4/8); trajectory-space
  points (0/8); split K/V bases (0/8).

Next gate: 4090 -- replicate at 4B-9B scale, 32k context, held-out documents.

**4090 tranche 1 COMPLETE (2026-08-28/29, Qwen3-4B, behavioral joint codec):**
- 16k (seed 11): full KV 14/14 (0.018); behavioral 16x/32x/64x all 14/14
  (0.022/0.012/0.061); variance 16x/32x 14/14, 64x 13/14 (0.176).
- 8k (seeds 11/12/13): behavioral 32x 42/42, 64x 40/42 (s13 dropped a
  confusable + person); variance 32x 41/42, 64x 36/42.
- Verdicts: compressibility grows with scale (A8, A12); fixed rank
  stretches 8k->16k (A21); behavioral = fidelity margin, not a recall
  cliff, at 4B (A8). Full tables in PREPRINT §8.
- Still open: second family, 32k, IFEval/leakage/RULER, matched-byte
  baselines, net accounting at 4B geometry (basis 340MB-1.36GB fp16).
